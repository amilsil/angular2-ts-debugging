import { Component } from '@angular/core';

@Component({
    selector: 'my-app',
    template: `
        <div class="container">
            debugging with ts
        </div>
        `
})

export class AppComponent {
    title = 'my breakpoint'
 }
